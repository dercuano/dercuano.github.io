function box(width, length, height) {
    return { surface: 2*(width*length + length*height + height*width)
           , volume: width * length * height
           , width, length, height
           }
}

function cube(side) {
    return box(side, side, side)
}

function cylinder(diameter,